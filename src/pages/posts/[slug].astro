---
import SiteLayout from "../../layouts/SiteLayout.astro";
import { posts } from "../../data/site";

export function getStaticPaths() {
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
---

<SiteLayout title={`${post.title} | 랑이슈타인`}>
  <article class="article-card">
    <p class="eyebrow">{post.category} · {post.publishedAt}</p>
    <h1 class="article-title">{post.title}</h1>
    <p class="article-description">{post.description}</p>

    <div class="tag-list">
      {post.tags.map((tag) => (
        <a href="/tags/" class="tag-chip">#{tag}</a>
      ))}
    </div>

    <div class="article-body">
      {post.body.map((paragraph: string) => (
        <p>{paragraph}</p>
      ))}
    </div>

    <div class="article-actions">
      <a href={`/categories/${post.categorySlug}/`} class="back-link">
        ← {post.category} 목록으로
      </a>
    </div>
  </article>
</SiteLayout>

<style>
  .article-card {
    padding: 1.8rem;
    border: 1px solid #dde5ed;
    border-radius: 24px;
    background: #ffffff;
    box-shadow: 0 10px 30px rgba(19, 36, 59, 0.06);
  }

  .eyebrow {
    margin: 0 0 0.45rem;
    color: #51719a;
    font-size: 0.9rem;
    font-weight: 700;
  }

  .article-title {
    margin: 0 0 0.65rem;
    font-size: clamp(2rem, 3vw, 3rem);
    line-height: 1.12;
    letter-spacing: -0.03em;
  }

  .article-description {
    margin: 0;
    color: #58697a;
    font-size: 1rem;
  }

  .tag-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.25rem;
  }

  .tag-chip {
    display: inline-flex;
    align-items: center;
    padding: 0.65rem 0.95rem;
    border-radius: 999px;
    background: #eef5fc;
    color: #244f84;
    font-size: 0.92rem;
    font-weight: 700;
  }

  .article-body {
    margin-top: 1.5rem;
  }

  .article-body p {
    margin: 0 0 1rem;
    color: #2e3c49;
    font-size: 1rem;
    line-height: 1.8;
  }

  .article-actions {
    margin-top: 1.75rem;
    padding-top: 1.25rem;
    border-top: 1px solid #e4ebf2;
  }

  .back-link {
    color: #285ea8;
    font-weight: 700;
  }

  @media (max-width: 820px) {
    .article-card {
      padding: 1.2rem;
      border-radius: 18px;
    }
  }
</style>